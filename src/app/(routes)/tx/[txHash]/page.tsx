import React from "react";

import Container from "@/components/container";

const TxPage = ({ params }: { params: { txHash: string } }) => {
    return <Container>TxPage</Container>;
};

export default TxPage;
